#include <stdio.h>
void main()
{
    // Print the name and check if printf was successful
    // printf returns the number of characters printed, which is always > 0 for non-empty strings
    if (printf("Raghvendra Singh\n")) {}
}